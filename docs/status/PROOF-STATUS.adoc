// SPDX-License-Identifier: MPL-2.0
// Copyright (c) Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= Proof Status — CLEAVE

[IMPORTANT]
====
*The canonical proof ledger is `docs/PROOF-NEEDS.adoc`.* Read that, not this.
This page is a summary view; where the two disagree, PROOF-NEEDS wins.

Until 2026-07-28 this file was an unedited RSR template reporting
"*Overall*: 0% proven" against categories (ABI/TP/INV/SEC/…) cleave does not
use — while the kernel had been demonstrated and its proofs green since
2026-07-09. Those numbers were never real; they are replaced below with what
actually runs.
====

== What is machine-verified today

Re-measured 2026-07-28 from a clean checkout:

[cols="3,2,3",options="header"]
|===
| Artifact | Result | How to reproduce

| Rust acceptance suite (KERN-1..7 + companions)
| 10/10 pass (debug); release drops the drop-bomb test by design
| `cargo test --all-targets`

| Linearity compile-fail doctest
| 1/1 pass (a second `teardown` does not compile)
| `cargo test --doc`

| Idris2 proof mirror (`cleave-proofs.ipkg`)
| 5/5 modules typecheck, exit 0
| `idris2 --typecheck cleave-proofs.ipkg`

| Trust base of the mirror
| zero `believe_me` / `assert_total` / `postulate`; every module `%default total`
| `grep -rn 'believe_me\|assert_total\|postulate' proofs/`
|===

NOTE: Gate the mirror with `--typecheck <ipkg>`, never a per-file
`idris2 --check` loop: `--check` exits 0 on module-load errors and on unsolved
holes (measured on Idris2 0.7.0 *and* 0.8.0), so a per-file loop is a fake gate.

== Scope of those results — read this before quoting them

Green above means *the kernel path*: exactly one demonstrated dial point (the
FFI-adjacent, in-process point, 2026-07-09). It is not the full surface.
Per `docs/PROOF-NEEDS.adoc`:

* `O-1`..`O-5`, `O-9`, `G-1`, `G-7` — closed *for the kernel path only*.
* `O-6` (joint completion, RC-9) and `O-7` (clean-XOR-rupture, RC-13 general)
  — open: they need two peers and a wire, neither of which exists yet.
* `O-8`/`G-5` — partial: groove's provider/CLI signing legs are in; the
  gossamer verify leg and burble producer-signing leg are not.
* `G-4` — open: RC-1..13 are vacuously green outside the kernel, because no
  real Hard-owns-Soft owns-graph exists in any consumer yet.
* `G-2`/`G-3` gossamer legs — merged but *unverified*: their designated CI
  (gossamer `e2e.yml`) has never parsed. See those rows in PROOF-NEEDS.

The bar for "demonstrated" is unchanged: real capability, soft *and* hard,
descent actually invoked, handle actually linear, proof guarding running code.
One point meets it. Saying "one point, no further" is the method.
