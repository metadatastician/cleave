// SPDX-License-Identifier: MPL-2.0
// Copyright (c) 2026 Jonathan D.A. Jewell (hyperpolymath) <j.d.a.jewell@open.ac.uk>
= Cleave — PROOF-NEEDS
:doctype: book
:revdate: 2026-06-16
:toc: macro
:toclevels: 2
:sectnums:

[abstract]
The proof obligations the cleave library must discharge, and the *honest gaps*
between the current state (proofs in gossamer, partly phantom/unwired) and a real
interface engine. This is the truth-ledger; nothing here is claimed done. Sources:
`docs/standards/RANKED-OWNERSHIP-CLEAVE.adoc`, the 2026-06-15 gossamer cleave-surface
audit, and the session handoff.

toc::[]

== Obligations (what a real cleave proves)

[cols="1,4",options="header"]
|===
| Obligation | Statement
| `O-1` Graceful teardown terminates | `gracefulTeardownTerminates`: a graceful, mutually-completed close reaches `perp` in finitely many steps (well-foundedness of the discharge measure). NOT `residueAlwaysZero`.
| `O-2` No owned residue at perp | Reaching `perp` for a subtree entails zero owned residue for that subtree (RC-7).
| `O-3` Ownership acyclic | The `owns` relation is well-founded, so ownership cycles (refcounting's classic leak) are unrepresentable (RC-2/RC-3). This is the cross-handle property per-handle linearity cannot see.
| `O-4` Partial-order fidelity | Release respects the order-dual of `owns` (children before parent); LIFO only in the degenerate linear-stack case (RC-8).
| `O-5` Handle linearity | Every handle is consumed exactly once (linear newtype, `let!`).
| `O-6` Joint completion | Clean-cleave is both peers at `perp` with dual ack; one-sided is half-closed residue (RC-9).
| `O-7` Clean-XOR-rupture | A lost final ack resolves as bounded retransmit OR escalation to survivor-owns-the-wipe — never dangling (RC-13).
| `O-8` Manifest authenticity | Declared capabilities match implementation; capability tokens are unspoofable on the wire.
| `O-9` Transmute-mode security | (ADOPTED-FOR-KERNEL 2026-07-09; normative text in RANKED-OWNERSHIP-CLEAVE v0.3 §TS) posture triple is a function of dial position, monotone, witnessed (TS-1..7). Kernel enforcement: KERN-7 + Posture.idr; full-surface proof still open.
|===

== Honest gaps (current state -> target)

[cols="1,1,4",options="header"]
|===
| Gap | Status | What must change

| `G-1` Teardown is flat-LIFO only
| *closed for the kernel path* (2026-07-09); general DM well-foundedness residual open
| CLOSED (kernel): `proofs/Cleave/Kernel/ResourceCleanup.idr` proves the plan is a *tree postorder* (children-before-parent, `planParentLast`), complete (`planComplete`), terminating (`gracefulTeardownTerminates`) and residue-zero at ⊥; `DMMultiset.idr` proves the step lemma (`teardownStepDecreasesDM`: each release is a Dershowitz-Manna step, all children strictly below). Runtime mirror: `src/teardown.rs`, KERN-3/KERN-4 green. RESIDUAL: the *general* DM accessibility proof (order type ω^ω) is NOT attempted — for a tree fixed at connect time, structural totality IS the specialised DM argument (RC-11's substitution); see `proofs/README.adoc` honesty ledger.

| `G-2` ResourceCleanup is unwired
| *CLOSED both paths* (kernel 2026-07-09; gossamer leg machine-verified 2026-08-03)
| KERNEL PATH CLOSED: the descent is the only teardown path in `src/teardown.rs`/`src/lib.rs` (KERN-4 asserts the order from the audit log). GOSSAMER PATH CLOSED: `gossamer_groove_disconnect_typed`'s flat wipe was replaced by a children-first postorder descent with per-subtree residue-0 assertion + audit ring — landed on gossamer `main` in PR #104 (2026-07-09), live at `src/interface/ffi/src/main.zig`. MACHINE VERIFICATION (the part this row long refused to claim): the designated verifier, gossamer's `e2e.yml` (`zig build test-integration`), had *never parsed* until repaired in gossamer#137; it then went red again when PR #141 merged an FFI surface that had never compiled, repaired in gossamer#143. As of 2026-08-03 the gate is GREEN on gossamer `main` at `5da3f63`: run https://github.com/metadatastician/gossamer/actions/runs/30842887080[`E2E + Safety` 30842887080] (and `ABI Typecheck Gate` run 30842887323) — an execution that actually runs the integration tests, satisfying the closure condition this row set.

| `G-3` Handles are bare I64 (non-linear)
| *CLOSED both paths* (kernel 2026-07-09; gossamer leg machine-verified 2026-08-03)
| KERNEL: `src/handle.rs` — `#[must_use]` non-Clone `Handle`, consumed by move in `Surface::teardown` (double-teardown is a compile error, `compile_fail` doctest; leak-without-teardown is a debug drop-bomb, KERN-2); proof mirror `proofs/Cleave/Kernel/HandleLinearity.idr` (`teardown : (1 h : Handle) -> Perp`). GOSSAMER: `GrooveHandle = enum(u64)` newtype + once-guarded session API — MERGED to gossamer `main` in PR #104 (2026-07-09), live at `src/interface/ffi/src/main.zig`. Machine verification arrived with G-2's: gossamer `main` @ `5da3f63`, run https://github.com/metadatastician/gossamer/actions/runs/30842887080[`E2E + Safety` 30842887080] + `ABI Typecheck Gate` run 30842887323 (both green 2026-08-03, after gossamer#137 made the workflow parse and gossamer#143 made the FFI surface compile). True exactly-once linearity remains the Idris/Ephapax layer's job (RC-5) — stated, not hidden.

| `G-4` RC-1..13 are vacuously green
| open
| There is no `owns`-graph today (flat 32-slot array / 10 targets). RC-2/RC-3 are *vacuously satisfied*; a green checker proves nothing yet. They go load-bearing only once a real Hard-owns-Soft subtree exists. Build the owns-tree first, then the invariants bite.

| `G-5` Manifests unsigned/untyped on the wire
| *design adopted + provider/CLI legs implemented* (2026-07-09); gossamer/burble verify legs pending
| Groove ADR 0010 + SPEC §2.1.5: Ed25519 over canonical JSON, detached `signature` member, public keys pinned in `groove-registry.json` (compile-time embedded). Reference provider signs (`GROOVE_SIGNING_KEY`); `groove validate --verify` verifies incl. pins; tamper/pin-mismatch tests green in groove CI. REMAINING for O-8: gossamer consumer verification (Zig `std.crypto.sign.Ed25519`, Phase 2) and burble producer signing (`:crypto` eddsa, stretch); `weft` in the original note is now `spline` (ADR 0009).

| `G-6` Proofs live on a phantom surface
| open
| In gossamer the proofs bind a phantom-template ABI (3/12 real `%foreign`), while the *true* cleave (the real Zig symbols) is unspecified. Rehome the **genuinely-generic** proofs + spec here and **rebuild wiring fresh** — do NOT lift the phantom.

| `G-7` Transmute-mode security unspecified in the normative spec
| *specified normatively; kernel-path enforcement demonstrated* (2026-07-09)
| TS-1..TS-7 promoted into `RANKED-OWNERSHIP-CLEAVE.adoc` v0.3 §Transmute-mode security (owner decision 2026-07-09, full-formal-assault round) with per-invariant enforcement status. Kernel: `posture_of` is the only posture constructor (TS-1), KERN-7 exercises TS-1/2/4/5; `proofs/Cleave/Kernel/Posture.idr` proves totality + monotonicity + preemption duality. TS-6 partial (witness at type level, not yet at a linear layer), TS-7 vacuous in-process — both stated in the spec, not claimed.
|===

== The bar: feasible -> demonstrated

`snifs` proved **one** wired point on the dial (SEC-1 in Agda, an ABI conformance gate,
21/21 in-BEAM tests). The cleave library proves the *surface* when a **SECOND point is
wired end-to-end**:

[%hardbreaks]
* a *real* capability (not a placeholder),
* *both* soft AND hard groove,
* the staircase teardown *actually invoked* (G-2 closed for this path),
* the linear handle *actually linear* (G-3 closed for this path),
* the proof *actually guarding running code* (not typecheck-only).

Until that second point is wired, "transmutable surface" is *feasible*, not
*demonstrated*. State that honestly in every status doc.

*Status 2026-07-09 — the kernel point is wired.* The kernel (`src/`,
`docs/KERNEL.adoc`) is the FFI-adjacent point: KERN-1..7 green locally
(`cargo test`), covering a real in-process capability (the ranked
owns-tree itself), BOTH soft and hard leases (KERN-5/KERN-6), the
staircase teardown actually invoked (KERN-3/KERN-4), the linear handle
actually linear (KERN-2 + `compile_fail` doctest), and the assertions
guarding running code. Proof mirrors typecheck in CI
(`cleave-proofs.ipkg`). The *protocol-end* real-capability demonstration
(gossamer↔burble over groove SPEC §4.6 leases, evidence script
`groove/examples/soft-hard-lease-demo.sh`) is authored on the estate
branch and verified against the reference provider; its burble/gossamer
legs gate on those repos' CI. README status is therefore "demonstrated at
one kernel point" — *no further*.

== Reuse, don't reinvent

[cols="1,3",options="header"]
|===
| Source | Reuse
| echo-types `Ordinal/CNF`, `RankPow` | omega.n / omega^k ranks (monotone embedding) — only when RC-11 permits ordinals (finite trees do not need them; the kernel needs none). *Audit deferred 2026-07-09: echo-types was not in session scope; add the repo before attempting the general DM accessibility proof (G-1 residual).*
| tropical-resource-typing `EchoBridge`, `MinPlus` | infinity = infeasible = bottom; residue->0 = grade->identity.
| ephapax `Region.idr` `RegionStack` | order-dual stack discipline.
| gossamer generic proofs (`HandleLinearity`, `ResourceCleanup`, `CapabilityAuthenticity`) | rehome the generic cores; rebuild wiring.
|===

NOTE: per estate policy, any echo-typed obligation MUST first audit `hyperpolymath/echo-types`,
reuse if applicable, extend upstream WITH proofs if not, then cross-document.
